# Generated by `boostdep --cmake accumulators`
# Copyright 2020 Peter Dimov
# Distributed under the Boost Software License, Version 1.0.
# https://www.boost.org/LICENSE_1_0.txt

cmake_minimum_required(VERSION 3.5...3.16)

project(boost_accumulators VERSION "${BOOST_SUPERPROJECT_VERSION}" LANGUAGES CXX)

set(BOOST_ACCUMULATORS_IS_ROOT OFF)
if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  set(BOOST_ACCUMULATORS_IS_ROOT ON)
endif()

option(BOOST_ACCUMULATORS_MRDOCS_BUILD "Build the target for MrDocs: see doc/mrdocs.yml" OFF)

if(NOT DEFINED BOOST_SRC_DIR AND DEFINED ENV{BOOST_SRC_DIR})
  set(DEFAULT_BOOST_SRC_DIR "$ENV{BOOST_SRC_DIR}")
else()
  set(DEFAULT_BOOST_SRC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../..")
endif()
set(BOOST_SRC_DIR ${DEFAULT_BOOST_SRC_DIR} CACHE STRING "Boost source dir to use when running CMake from this directory")

# Dependencies (Boost::* names) for INTERFACE library and for BOOST_INCLUDE_LIBRARIES when standalone
set(BOOST_ACCUMULATORS_DEPENDENCIES
  Boost::array
  Boost::assert
  Boost::circular_buffer
  Boost::concept_check
  Boost::config
  Boost::core
  Boost::fusion
  Boost::iterator
  Boost::mpl
  Boost::numeric_conversion
  Boost::numeric_ublas
  Boost::parameter
  Boost::preprocessor
  Boost::range
  Boost::serialization
  Boost::throw_exception
  Boost::tuple
  Boost::type_traits
  Boost::typeof
)

foreach(BOOST_ACCUMULATORS_DEPENDENCY ${BOOST_ACCUMULATORS_DEPENDENCIES})
  if(BOOST_ACCUMULATORS_DEPENDENCY MATCHES "^[ ]*Boost::([A-Za-z0-9_]+)[ ]*$")
    list(APPEND BOOST_ACCUMULATORS_INCLUDE_LIBRARIES ${CMAKE_MATCH_1})
  endif()
endforeach()

# Paths under libs/ must use slashes for nested libraries (BoostRoot.cmake)
list(REMOVE_ITEM BOOST_ACCUMULATORS_INCLUDE_LIBRARIES numeric_conversion numeric_ublas)
list(APPEND BOOST_ACCUMULATORS_INCLUDE_LIBRARIES numeric/conversion numeric/ublas)

set(BOOST_INCLUDE_LIBRARIES ${BOOST_ACCUMULATORS_INCLUDE_LIBRARIES})
set(BOOST_EXCLUDE_LIBRARIES accumulators)

if(BOOST_ACCUMULATORS_IS_ROOT)
  set(CMAKE_FOLDER Dependencies)
  if(NOT IS_ABSOLUTE ${BOOST_SRC_DIR})
    set(BOOST_SRC_DIR "${CMAKE_CURRENT_SOURCE_DIR}/${BOOST_SRC_DIR}")
  endif()

  set(BOOST_SRC_DIR_IS_VALID ON)
  foreach(F "CMakeLists.txt" "Jamroot" "boost-build.jam" "bootstrap.sh" "libs")
    if(NOT EXISTS "${BOOST_SRC_DIR}/${F}")
      message(STATUS "${BOOST_SRC_DIR}/${F} does not exist. Fallback to find_package.")
      set(BOOST_SRC_DIR_IS_VALID OFF)
      break()
    endif()
  endforeach()

  if(BOOST_SRC_DIR_IS_VALID)
    if(BUILD_SHARED_LIBS)
      set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
    endif()
    # Exclude this library when pulling Boost via add_subdirectory (same pattern as Boost.URL)
    set(BOOST_EXCLUDE_LIBRARIES ${PROJECT_NAME})
    set(PREV_BUILD_TESTING ${BUILD_TESTING})
    set(BUILD_TESTING OFF CACHE BOOL "Build the tests." FORCE)
    add_subdirectory(${BOOST_SRC_DIR} Dependencies/boost EXCLUDE_FROM_ALL)
    set(BUILD_TESTING ${PREV_BUILD_TESTING} CACHE BOOL "Build the tests." FORCE)
    set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${BOOST_SRC_DIR}/tools/cmake/include")
  else()
    find_package(Boost REQUIRED)
    foreach(BOOST_INCLUDE_LIBRARY ${BOOST_INCLUDE_LIBRARIES})
      if(NOT TARGET Boost::${BOOST_INCLUDE_LIBRARY})
        add_library(Boost::${BOOST_INCLUDE_LIBRARY} ALIAS Boost::headers)
      endif()
    endforeach()
  endif()
  unset(CMAKE_FOLDER)
endif()

add_library(boost_accumulators INTERFACE)
add_library(Boost::accumulators ALIAS boost_accumulators)

target_include_directories(boost_accumulators INTERFACE include)

target_link_libraries(boost_accumulators INTERFACE ${BOOST_ACCUMULATORS_DEPENDENCIES})

if(BOOST_ACCUMULATORS_MRDOCS_BUILD)
  file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/mrdocs.cpp" "#include <boost/accumulators/accumulators.hpp>\n")
  add_library(boost_accumulators_mrdocs "${CMAKE_CURRENT_BINARY_DIR}/mrdocs.cpp")
  set_target_properties(boost_accumulators_mrdocs PROPERTIES CXX_STANDARD 17 CXX_STANDARD_REQUIRED YES)
  target_link_libraries(boost_accumulators_mrdocs PRIVATE boost_accumulators)
  if(DEFINED ENV{BOOST_SRC_DIR})
    file(GLOB BOOST_MODULE_PATHS "$ENV{BOOST_SRC_DIR}/libs/*/include")
    target_include_directories(boost_accumulators_mrdocs PRIVATE ${BOOST_MODULE_PATHS})
  endif()
  return()
endif()

if(BUILD_TESTING AND EXISTS "${CMAKE_CURRENT_SOURCE_DIR}/test/CMakeLists.txt")

  add_subdirectory(test)

endif()
